import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'flex-start',
        alignItems: 'center',
        backgroundColor: '#F5FCFF' // crossfile-link3919
    },
    welcome: {
        fontSize: 40,
        textAlign: 'center',
        margin: 10,
        zIndex: 2
    },
});

export default styles

